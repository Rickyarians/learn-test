export interface Colors {
  primary: string;
  shadow_primary: string;
  warning: string;
  danger: string;
  success: string;
  background_color: string;
  text: string;
  light: string;
  grey: string;
}

export enum RadiusSizes {
  small = 5,
  medium = 8,
  large = 15,
  extra_large = 20,
}
